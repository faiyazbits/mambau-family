/*
 * MemberView Messages
 *
 * This contains all the text for the MemberView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MemberView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MemberView container!',
  },
});
